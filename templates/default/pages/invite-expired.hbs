{{!-- Invitation expired or invalid token --}}
<div class="text-center">
  <h1>{{t "invitation.expired_title"}}</h1>
  <p class="text-muted mb-4">{{t "invitation.expired_message"}}</p>
  <p class="text-muted">{{t "invitation.contact_admin"}}</p>
</div>
