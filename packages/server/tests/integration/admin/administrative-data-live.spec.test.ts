/**
 * Collect the immutable administrative-data contract in the service-backed integration project.
 * The project environment enables the production adapter before this module is evaluated.
 */
import '../../unit/admin/administrative-data-contract.spec.test.js';
