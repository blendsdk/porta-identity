/**
 * Load user user profile
 */
export const loadUserProfile = () => {
    return new Promise<void>((resolve, _reject) => {
        setTimeout(() => {
            resolve();
        }, 1000);
    });
};
