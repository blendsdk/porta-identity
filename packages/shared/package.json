{
    "name": "@porta/shared",
    "version": "1.0.133",
    "main": "dist/index.js",
    "scripts": {
        "ls": "docker container ls",
        "mkpack": "rm -fR *.tgz && yarn pack",
        "build": "rm -fR ./dist && tsc",
        "test": "DEBUG=true jest --detectOpenHandles --runInBand --passWithNoTests --forceExit $TEST_FILTER",
        "watch": "tsc -w"
    },
    "types": "./dist/index.d.ts",
    "dependencies": {
        "@blendsdk/stdlib": "3.2.231"
    }
}