# Generated Project Configuration

> **🔄 Updated by `analyze_project`** (incremental update)
> **Scanned:** 2026-04-20
> **Changes detected:**
> - Project Overview: re-scan confirmed name; type kept as `api`
> - Toolchain: preserved curated detail (Koa, node-oidc-provider, pg, ioredis, pino, zod, tsx, jose)
> - Commands: preserved curated scripts; added `porta init`, `porta login` bootstrap commands
> - Project Structure: updated `src/` tree with admin-auth middleware, CLI HTTP client, new route files
> - CLI architecture: updated from direct-DB to HTTP-first (dual-mode bootstrap)
> - Special Rules: updated test counts, CLI architecture description, added admin-auth description

> **Auto-generated by `analyze_project`** (incremental update)
> **Project:** porta
> **Type:** api
> **Last Updated:** 2026-04-20 (admin-auth documentation update)

Save this content to `.clinerules/project.md` in your project root,
then review and adjust the values as needed.

---

## 🚨 MANDATORY: Load CodeOps Rules Before Any Work

**Before ANY planning or implementation, the AI agent MUST load these rules
using the codeops-mcp tools:**

1. `get_rule("agents")` — Load agent behavior rules **(REQUIRED FIRST)**
2. `get_rule("code")` — Load coding standards
3. `get_rule("testing")` — Load testing workflows
4. `get_rule("git-commands")` — Load git commit protocols

These rules are **mandatory** and must be consulted before every task.
**Do NOT skip this step. Do NOT proceed without reading these documents.**

---

## Project Overview

- **Name:** porta
- **Description:** Multi-tenant OIDC provider built on node-oidc-provider + Koa + TypeScript + PostgreSQL + Redis. Provides organization-scoped authentication, user management, RBAC, custom claims, and 2FA.
- **Type:** api

## Toolchain

- **Language(s):** TypeScript (ES2022, NodeNext module resolution, strict mode)
- **Framework(s):** Koa, node-oidc-provider, ESLint, Prettier
- **Package Manager:** yarn (Classic 1.22, NOT npm, NOT Berry)
- **Test Framework:** Vitest
- **Runtime:** Node.js >=22.0.0
- **Database:** PostgreSQL 16 (via pg)
- **Cache/Sessions:** Redis 7 (via ioredis)
- **Logger:** pino (pino-pretty in dev, silent in test, JSON in prod)
- **Config Validation:** zod
- **Dev Server:** tsx watch
- **OIDC Engine:** node-oidc-provider 9.x

**Manifest files found:** package.json, Makefile, tsconfig.json, docker/docker-compose.yml

## Commands

All commands assume execution from the project root. Prefix all shell commands with `clear && sleep [delay] &&` (see Terminal Delay below).

### Terminal Delay

- **Delay (seconds):** 3
- The `clear` ensures a clean terminal; the `sleep` gives VS Code time to initialize the terminal before the command runs.
- Adjust the delay for your environment: `1` for fast machines, `3` (default) for normal, `5` for slower environments.
- All command examples below use `sleep 3` — replace `3` with your configured delay.

### Build

```bash
clear && sleep 3 && yarn build
```

### Test

```bash
# Run unit + integration tests (default)
clear && sleep 3 && yarn test

# Run ALL vitest suites (unit + integration + e2e + pentest)
clear && sleep 3 && yarn test:all

# Run unit tests only
clear && sleep 3 && yarn test:unit

# Run integration tests (requires Docker services)
clear && sleep 3 && yarn test:integration

# Run E2E tests (requires Docker services)
clear && sleep 3 && yarn test:e2e

# Run penetration tests (requires Docker services)
clear && sleep 3 && yarn test:pentest

# Run UI tests (Playwright, requires Docker services + Chromium)
clear && sleep 3 && yarn test:ui
```

### Lint

```bash
clear && sleep 3 && yarn lint
```

### Verify (before commit)

```bash
# Full verification — run this before any git commit
clear && sleep 3 && yarn verify
```

### Docker

```bash
# Start infrastructure services (Postgres, Redis, MailHog)
clear && sleep 3 && yarn docker:up

# Stop infrastructure services
clear && sleep 3 && yarn docker:down
```

### Dev Server

```bash
# Start dev server with hot-reload (requires docker:up first)
clear && sleep 3 && yarn dev
```

## Project Structure

### Type: Single repository

### Directory Layout

```
docker/              # Docker Compose infrastructure
  docker-compose.yml # Postgres 16, Redis 7, MailHog
src/
  config/            # Zod-validated config schema + loader
    schema.ts        # Config schema with zod validation (includes cookieKeys)
    index.ts         # Config loader (dotenv + zod parse + fail-fast)
  lib/               # Core library modules
    logger.ts        # Pino logger (dev/prod/test modes)
    database.ts      # PostgreSQL connection pool (pg)
    redis.ts         # Redis client (ioredis)
    migrator.ts      # Programmatic migration runner
    system-config.ts # System config service (typed getters, 60s cache)
    signing-keys.ts  # ES256 key generation, PEM↔JWK, DB load, auto-bootstrap
    audit-log.ts     # Generic audit log writer (fire-and-forget)
  middleware/         # Koa middleware
    error-handler.ts # Global error handler
    request-logger.ts # Request logging with X-Request-Id
    health.ts        # GET /health endpoint (DB + Redis checks)
    tenant-resolver.ts # Cache-first org resolution, status differentiation
    admin-auth.ts    # JWT Bearer auth for /api/admin/* (ES256, RBAC porta-admin role)
    client-secret-hash.ts # SHA-256 pre-hash for OIDC client_secret_post
    oidc-cors.ts     # CORS handler for OIDC endpoints
    root-page.ts     # Neutral GET /, /robots.txt, /favicon.ico (no product leakage)
  oidc/              # OIDC provider modules
    configuration.ts # Provider configuration builder (PKCE, scopes, claims, TTLs)
    provider.ts      # Provider factory (creates configured Provider instance)
    postgres-adapter.ts # PostgreSQL adapter for long-lived OIDC artifacts
    redis-adapter.ts # Redis adapter for short-lived OIDC artifacts
    adapter-factory.ts # Hybrid adapter factory (routes by model name)
    client-finder.ts # Client metadata lookup from DB
    account-finder.ts # User/account claims lookup from DB (RBAC + custom claims integration)
  organizations/     # Organization (tenant) management module (RD-04)
    index.ts         # Barrel export — public API for the module
    types.ts         # Organization types, interfaces, row mapping
    slugs.ts         # Slug generation (from name) and validation
    errors.ts        # OrganizationNotFoundError, OrganizationValidationError
    repository.ts    # PostgreSQL CRUD (insert, find, update, list, slugExists)
    cache.ts         # Redis cache (get/set/invalidate by slug and ID)
    service.ts       # Business logic: CRUD, status lifecycle, audit logging
  applications/      # Application management module (RD-05)
    index.ts         # Barrel export
    types.ts         # Application + ApplicationModule types, interfaces, row mapping
    slugs.ts         # Application slug generation and validation
    errors.ts        # ApplicationNotFoundError, ApplicationValidationError
    repository.ts    # PostgreSQL CRUD for apps and modules
    cache.ts         # Redis cache (get/set/invalidate)
    service.ts       # Business logic: CRUD, status lifecycle, modules
  clients/           # Client & secret management module (RD-05)
    index.ts         # Barrel export
    types.ts         # Client/secret types, interfaces, row mapping
    crypto.ts        # Client ID/secret generation, Argon2id hash/verify
    validators.ts    # Redirect URI validation, default grant types
    errors.ts        # ClientNotFoundError, ClientValidationError
    repository.ts    # PostgreSQL CRUD for clients
    secret-repository.ts # PostgreSQL CRUD for client secrets
    cache.ts         # Redis cache for clients
    service.ts       # Client business logic: CRUD, status, OIDC mapping
    secret-service.ts # Secret lifecycle: generate, verify, revoke, cleanup
  users/             # User management module (RD-06)
    index.ts         # Barrel export — public API for the module
    types.ts         # User types, UserRow, mapRowToUser, status lifecycle
    errors.ts        # UserNotFoundError, UserValidationError
    password.ts      # Argon2id hash/verify, NIST SP 800-63B validation
    repository.ts    # PostgreSQL CRUD with dynamic UPDATE, pagination/search/sort
    cache.ts         # Redis by-ID with Date deserialization, graceful degradation
    claims.ts        # OIDC Standard Claims builder with scope-based filtering
    service.ts       # Business logic: CRUD, 6 status transitions, password, login tracking
  auth/              # Authentication workflow module (RD-07)
    index.ts         # Barrel export — public API for the module
    csrf.ts          # CSRF token generation and validation
    tokens.ts        # Secure token generation (magic link, password reset, invitation)
    token-repository.ts # Token storage and validation in PostgreSQL
    rate-limiter.ts  # Redis-backed rate limiting for auth endpoints
    email-transport.ts # SMTP transport abstraction (Nodemailer)
    email-renderer.ts # Template-based email rendering (Handlebars)
    email-service.ts # High-level email sending (magic link, reset, invitation)
    i18n.ts          # Internationalization (locale loading, translation lookup)
    template-engine.ts # HTML template engine for login/auth pages
    magic-link-session.ts # Magic link session helpers
  rbac/              # RBAC module (RD-08)
    index.ts         # Barrel export — public API for the module
    types.ts         # Role, Permission, RolePermissionMapping types and row mapping
    errors.ts        # RoleNotFoundError, PermissionNotFoundError, RbacValidationError
    slugs.ts         # RBAC slug generation and validation (roles + permissions)
    role-repository.ts    # PostgreSQL CRUD for roles
    permission-repository.ts # PostgreSQL CRUD for permissions
    mapping-repository.ts # PostgreSQL CRUD for role-permission + user-role mappings
    cache.ts         # Redis cache for roles, permissions, user roles
    role-service.ts  # Role business logic: CRUD, permission assignment
    permission-service.ts # Permission business logic: CRUD, usage checks
    user-role-service.ts  # User-role assignment: assign, remove, list
  custom-claims/     # Custom claims module (RD-08)
    index.ts         # Barrel export — public API for the module
    types.ts         # ClaimDefinition, UserClaimValue types and row mapping
    errors.ts        # ClaimNotFoundError, ClaimValidationError
    validators.ts    # Claim value validation against definition rules
    repository.ts    # PostgreSQL CRUD for claim definitions + user values
    cache.ts         # Redis cache for claim definitions and user values
    service.ts       # Business logic: definitions CRUD, user values, validation
  two-factor/        # Two-factor authentication module (RD-12)
  routes/            # API route handlers
    organizations.ts # /api/admin/organizations — 10 endpoints, Zod validation
    applications.ts  # /api/admin/applications — 11 endpoints, Zod validation
    clients.ts       # /api/admin/clients — 10 endpoints, Zod validation
    users.ts         # /api/admin/organizations/:orgId/users — 13 endpoints, Zod validation
    roles.ts         # /api/admin/applications/:appId/roles — 9 endpoints (RD-08)
    permissions.ts   # /api/admin/applications/:appId/permissions — 6 endpoints (RD-08)
    user-roles.ts    # /api/admin/organizations/:orgId/users/:userId/roles — 4 endpoints (RD-08)
    custom-claims.ts # /api/admin/applications/:appId/claims — 9 endpoints (RD-08)
    config.ts        # /api/admin/config — system configuration management
    keys.ts          # /api/admin/keys — signing key management
    audit.ts         # /api/admin/audit — audit log viewer with filters
    two-factor.ts    # /interaction/:uid/two-factor/* — 2FA interaction routes
    interactions.ts  # OIDC interaction routes (login, consent)
    magic-link.ts    # Magic link request and verification routes
    password-reset.ts # Password reset request and verification routes
    invitation.ts    # User invitation routes
  cli/               # CLI admin tooling module (RD-09, RD-13)
    index.ts         # CLI entry point with yargs setup, GlobalOptions, buildCli()
    bootstrap.ts     # Dual-mode bootstrap: withBootstrap() (direct-DB) + withHttpClient() (HTTP)
    http-client.ts   # AdminHttpClient: authenticated HTTP requests, auto token refresh, error mapping
    token-store.ts   # File-based credential storage at ~/.porta/credentials.json (0600 perms)
    pkce.ts          # PKCE code_verifier + code_challenge (S256) generation
    output.ts        # Output helpers: printTable, printJson, success, warn, error, formatDate
    error-handler.ts # withErrorHandling() wrapper with domain error mapping + exit codes
    prompt.ts        # Confirmation + input + password prompts with readline
    commands/        # Command modules (one per domain)
      init.ts        # porta init — bootstrap admin infrastructure (direct-DB, one-time)
      login.ts       # porta login — OIDC Auth Code + PKCE flow via browser
      logout.ts      # porta logout — clear stored credentials
      whoami.ts      # porta whoami — display current identity
      health.ts      # porta health — DB + Redis connectivity (HTTP or --direct)
      migrate.ts     # porta migrate up/down/status — migration management (direct-DB)
      seed.ts        # porta seed run — development seed data (direct-DB)
      keys.ts        # porta keys list/generate/rotate — ES256 signing keys (HTTP)
      config.ts      # porta config list/get/set — system configuration (HTTP)
      audit.ts       # porta audit list — audit log viewer (HTTP)
      org.ts         # porta org create/list/show/update/suspend/activate/archive/branding (HTTP)
      app.ts         # porta app create/list/show/update/archive + module/role/permission/claim (HTTP)
      app-module.ts  # porta app module add/remove/list — application modules (HTTP)
      app-role.ts    # porta app role create/list/show/update/archive/assign-perm/remove-perm (HTTP)
      app-permission.ts # porta app permission create/list/show/archive (HTTP)
      app-claim.ts   # porta app claim create/list/show/archive — custom claim definitions (HTTP)
      client.ts      # porta client create/list/show/update/revoke + secret management (HTTP)
      client-secret.ts # porta client secret generate/list/revoke — client secrets (HTTP)
      user.ts        # porta user create/invite/list/show/update + 6 status + set-password + roles/claims/2fa (HTTP)
      user-role.ts   # porta user roles assign/remove/list — user role assignments (HTTP)
      user-claim.ts  # porta user claims set/remove/list — user custom claim values (HTTP)
  server.ts          # Koa app factory with middleware stack + routes + OIDC mounting
  index.ts           # Entry point: DB → Redis → keys → TTLs → provider → server
templates/           # Handlebars templates for auth UI pages
  default/           # Default template set
locales/             # i18n locale files
  default/           # Default locale set
    en/              # English translations
playground/          # Static OIDC SPA playground (RD-14)
playground-bff/      # BFF playground (RD-15)
scripts/             # Helper shell/TS scripts (seed, reset, smoke)
tests/
  unit/              # Unit tests (no external services needed)
  integration/       # Integration tests (require Docker services)
  e2e/               # End-to-end tests
  pentest/           # Penetration test suites
  ui/                # Playwright UI specs
  fixtures/          # Test fixtures
  helpers/           # Test helpers
plans/               # Implementation plans (make_plan output)
  _archive/          # Archived completed plans (RD-01 through RD-12 + misc)
  bff-playground/    # Active BFF playground plan
  client-login-methods/ # Completed login-methods plan
  erp-claims-playground/ # Active ERP claims playground plan
  magic-link-cleanup/ # Active magic-link cleanup plan
requirements/        # Requirements documents (RD-01 through RD-17)
migrations/          # SQL migration files (001–014)
dist/                # TypeScript build output (gitignored)
```

## Coding Conventions

### Naming

- **Files:** kebab-case (e.g., `error-handler.ts`, `request-logger.ts`)
- **Classes:** PascalCase
- **Functions/Methods:** camelCase (e.g., `connectDatabase`, `getPool`)
- **Constants:** camelCase for config, UPPER_SNAKE_CASE for true constants
- **Types/Interfaces:** PascalCase (e.g., `AppConfig`)

### Import Style

- Use `.js` extensions in imports (NodeNext module resolution)
- Named imports preferred: `import { pino } from 'pino'`
- Type-only imports: `import type { Middleware } from 'koa'`

## Git & Commit Conventions

### Commit Scope

```
# Use module/feature as scope:
# feat(scaffolding): description
# feat(oidc): description
# feat(organizations): description
# feat(users): description
# feat(auth): description
# feat(rbac): description
# feat(cli): description
# fix(auth): description
```

### Branch Strategy

- **Main branch:** `main`
- **Feature branches:** `feature/[name]`

## Special Rules (Project-Specific)

- **Koa, NOT Express** — required for node-oidc-provider compatibility
- **Yarn Classic 1.22** — enforced via .npmrc `engine-strict=true`
- **Graceful shutdown** — SIGTERM/SIGINT → close server → disconnect DB → disconnect Redis
- **Health check** — `GET /health` must check server + DB + Redis
- **Config fail-fast** — missing/invalid env vars must cause immediate exit with clear error
- **Logger before config** — logger uses process.env directly (no config dependency)
- **17 Requirements Documents** — RD-01 through RD-17 in `requirements/` (RD-13 admin-auth CLI v2, RD-14 playground application, RD-15 playground infrastructure, RD-16 scope translation/UI polish, RD-17 setup documentation)
- **Hybrid OIDC adapters** — Redis for short-lived (Session, Interaction, AuthorizationCode, ReplayDetection, ClientCredentials, PushedAuthorizationRequest), PostgreSQL for long-lived (AccessToken, RefreshToken, Grant). Session `destroy()` cascades grant/token deletion across both stores for explicit logout; natural session expiry preserves tokens for refresh flows. Opportunistic `purgeExpired()` fire-and-forget cleanup on auth flow start.
- **ES256 signing** — ECDSA P-256 for token signing, keys stored as PEM in DB
- **Path-based multi-tenancy** — `/:orgSlug/*` prefix for all OIDC endpoints
- **System config service** — reads TTLs from `system_config` table with 60s in-memory cache
- **Organization module** — Full CRUD + status lifecycle (active/suspended/archived), Redis-cached tenant resolution, audit logging, super-admin protected API at `/api/admin/organizations`
- **RBAC module** — Roles/permissions per application, user-role assignments per org, role-permission mappings, cached lookups
- **Custom claims module** — Claim definitions per application, user claim values, type-validated, cached
- **Functional code style** — standalone exported functions, not classes
- **Admin auth middleware** — JWT Bearer token authentication for `/api/admin/*` routes. Validates ES256 tokens against Porta's own signing keys (self-authentication), verifies issuer matches super-admin org, checks user is active and belongs to super-admin org, checks `porta-admin` RBAC role. Sets `ctx.state.adminUser` for downstream handlers. Unauthenticated metadata endpoint at `GET /api/admin/metadata` for CLI login discovery.
- **CLI module** — yargs-based admin CLI (`porta`), 14 top-level commands, dual-mode bootstrap: `withBootstrap()` (direct-DB for init/migrate/seed) and `withHttpClient()` (authenticated HTTP for all other commands). `porta init` bootstraps admin app + RBAC + PKCE client + first user. `porta login` runs OIDC Auth Code + PKCE via browser, stores credentials at `~/.porta/credentials.json`. `AdminHttpClient` handles Bearer token injection, auto-refresh, and HTTP error mapping. Table + JSON output, confirmation prompts, 220+ CLI tests
- **Two-factor module** — Email OTP, TOTP (authenticator), recovery codes, per-org policy, login flow integration, templates, i18n, CLI admin (status/disable/reset), AES-256-GCM secret encryption, Argon2id recovery hashing
- **Login methods module** — Per-client `login_methods` override (NULL = inherit from org) + per-org `default_login_methods` (NOT NULL, DB DEFAULT `{password,magic_link}`). Resolution via `resolveLoginMethods(org, client)`. Enforced on 5 endpoints (interaction `/login`, `/magic-link`, forgot-password GET/POST, reset-password POST) **before** CSRF/rate-limit/user-lookup with 403 + audit event `security.login_method_disabled`. Login template renders 4 modes (both / password-only / magic-link-only / empty fallback) and honours OIDC `login_hint`. Admin API + CLI support per-client override get/set/clear.
- **Test suite** — 2,629 unit+integration tests across 149 files passing, zero failures (`yarn verify` ~90s). Plus 20+ E2E test files, 32+ pentest files, 20+ UI Playwright specs.
- **Root-page handler** — `GET /`, `/robots.txt`, `/favicon.ico` served by `src/middleware/root-page.ts` with a neutral no-leakage response (no product/vendor strings, noindex headers, CSP `default-src 'none'`). Mounted after `/health` and before any `/:orgSlug/*` catch-all.
- **Pentest coverage** — Auth bypass (SQL injection, brute force, timing, session), magic link attacks (prediction, replay, host injection, enumeration), injection (SQL, XSS, CRLF, SSTI), crypto (JWT algo confusion, manipulation, key confusion), admin security (unauthorized access, privilege escalation, IDOR, mass assignment), multi-tenant (cross-tenant auth, enumeration, slug injection), infrastructure (HTTP headers, CORS, method tampering, info disclosure)

## Cross-References

The generic rule files that read this `project.md`:

- **make_plan.md** — Uses verify command, file paths, commit scope, task file path patterns
- **code.md** — Uses language conventions, architecture rules
- **testing.md** — Uses test commands, test locations, test framework
- **git-commands.md** — Uses commit scope, verify command
- **agents.md** — Uses shell commands, verify command

Project-specific rule files in `.clinerules/`:

- **security-principal.md** — **SUPREME PRIORITY.** Security is the principal constraint for ALL work on Porta. No implementation (bug fix, test fix, feature, refactor, removal) may degrade Porta's security posture. Defines 12 security domains (auth flows, token security, crypto standards, input validation, multi-tenant isolation, rate limiting, RBAC, session management, transport security, info leakage prevention, 2FA integrity, secret management), a mandatory Security Impact Assessment (10-question checklist), explicit Forbidden Actions, and a Conflict Resolution Protocol (STOP → EXPLAIN → PROPOSE → ESCALATE). The agent must refuse and explain if a task would weaken security. Pentest suite is the codified security baseline. See `.clinerules/security-principal.md` for the full rule.

- **docs-maintenance.md** — Mandatory documentation review & update rule. Enforces a final documentation phase in every formal plan and a docs check before `attempt_completion` in ad-hoc tasks. Includes **Cross-Document Consistency** (duplication map for env vars, CLI commands, Docker setup across multiple doc files) and **Source-of-Truth Verification** (verify docs against actual code, not other docs). See `.clinerules/docs-maintenance.md` for the full rule, coverage checklist, and quality standards.
